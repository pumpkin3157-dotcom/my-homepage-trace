# 2026-08-12 工作日志

## 任务：部署个人主页 "Trace·轨迹" 并修复加载问题

### 项目背景
- 项目路径：`D:\Webfront\my-homepage-trace`
- 手账风格个人主页，含求学/旅行/照片/阅读/关于 5 个板块
- GitHub 仓库：`pumpkin3157-dotcom/my-homepage-trace`

### 部署方式
- 使用 CloudStudio 部署（workbuddy_cloudstudio_deploy 工具）
- 分享链接：`https://4bb2db021aeb485c891ded2d2d591da4.gz4.agentos-app.net`
- GitHub Pages 已被用户设为 None，不再使用

### 修复的问题（按时间顺序）
1. **图片过大**：38 张照片共 140MB，单张最大 14MB。用 Pillow 压缩到 ~10MB，单张 <600KB，背景 PNG 转 JPG。
2. **外部字体依赖**：原 `fonts.loli.net`（Google Fonts 镜像，国内不通）导致转圈。换成 `fonts.googleapis.cn`（Google 官方中国 CDN），字体名保持不变（Caveat、Ma Shan Zheng 等）。
3. **等待机制缺陷**：JS 里 `window.load` 才设 `body opacity=1`，导致必须等所有图片加载完才显示内容。改为点击 "Let's Go" 立即显示。
4. **背景滚动卡顿**：`background-attachment: fixed` 是移动端性能杀手。改用 `html::before` 固定伪元素实现。
5. **图片一次性加载**：给书籍封面、塔罗牌、照片轮播共 20+ 张图加 `loading="lazy"`。

### 关键经验
- 用户对字体有明确审美要求，不要擅自替换字体，优先换 CDN 来源而不是换字体。
- 部署后必须实测，不能只看部署工具返回的 verified 字段。
- **重要坑**：`document.body.style.opacity = '0'` 会把 body 里所有内容（含开场动画 intro-overlay）一起隐藏。开场动画本身就是 z-index 9999 的固定层，不需要 body opacity 来隐藏内容。这个机制是原始代码的 bug。
- 背景图用 `background-attachment: fixed` 在移动端滚动卡顿，改用 `html::before` 固定伪元素（z-index -1）+ 移除 body 背景。
- CloudStudio 部署偶发 "exec failed (400)"，用干净目录（排除 .git/.workbuddy）重新部署可生成新 sandbox。

### 最终部署
- 新分享链接：`https://19c0cb5a6bca467897e8bea9a84270ae.gz3.agentos-app.net`
- 旧链接 `https://4bb2db021aeb485c891ded2d2d591da4.gz4.agentos-app.net` 已废弃（有 body opacity bug）
